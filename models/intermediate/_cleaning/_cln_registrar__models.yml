version: 2

models:

  - name: cln_dim_persons
    docs:
      show: false
    config:
      tags: ["cleaning"]
      group: registrar

  - name: cln_dim_majors
    docs:
      show: false
    config:
      tags: ["cleaning"]
      group: registrar

  - name: cln_dim_minors
    docs:
      show: false
    config:
      tags: ["cleaning"]
      group: registrar
